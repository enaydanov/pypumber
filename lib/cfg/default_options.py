#! /usr/bin/env python

from options import Options

DEFAULT_OPTIONS = Options(
    strict=False,
    lang='en',
    dry_run=False,
    source=True,
    snippets=True,
    multiline=True,
)
